{
  "name": "intellisnake",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p tsc build:watch serve",
    "start:dev": "run-p tsc build:watch serve mock-server",
    "tsc": "tsc",
    "serve": "live-server dist",
    "build": "cp -R ./public/* ./dist",
    "build:watch": "watch \"npm run build\" ./public",
    "mock-server": "node ./websocket-server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "npm-watch": "^0.6.0",
    "typescript": "^3.7.4",
    "watch": "^1.0.2",
    "ws": "^7.2.1"
  }
}
